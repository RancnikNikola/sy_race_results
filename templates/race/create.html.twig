{% extends 'base.html.twig' %}

{% block body %}

    {# <div class="container">
        <h3>Create new Race</h3>
        <div class="">
            {{ form_start(form) }}
               <div class="">
                    {{ form_label(form.raceName) }}
                    {{ form_widget(form.raceName) }}
               </div>
               <div class="">  
                    {{ form_label(form.date) }}
                    {{ form_widget(form.date) }}
               </div>

            {{ form_end(form) }}
        </div>
    </div> #}

    <div class="container">
    <div class="row">
        <h3 class="">Create New Race</h3>
        <div class="col-md-6 offset-md-3">
        {{ form_start(form) }}
            <div class="form-group">
                {{ form_label(form.raceName )}}
                {{ form_widget(form.raceName, {'attr':{'class':'form-control'}}) }}
            </div>
             <div class="form-group mt-3">
                {{ form_label(form.date )}}
                {{ form_widget(form.date, {'attr':{'class':'border-0'}}) }}
            </div>
            <div class="form-group">
               <button type="submit" class="btn btn-primary btn-block mt-3">Create</button>
            </div>
        {{ form_end(form) }}
        </div>
    </div>
</div>


{% endblock %}